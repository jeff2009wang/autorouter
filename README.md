# 😼 NekoBrain (Intelligent LLM Router)

NekoBrain 是一个智能模型网关，利用本地视觉模型 (Qwen2.5-VL) 和语义分析 (CrossEncoder)，将用户请求智能路由到最合适的后端模型（如 Gemini, GPT-5.1, MiniMax 等）。

## ✨ 核心特性

*   **本地 OCR 增强**：使用 `Qwen2.5-VL-3B` 在本地解析图像内容（代码、公式、文档），避免云端模型产生“一般场景”的幻觉，同时降低隐私风险 [1]。
*   **语义路由**：基于 `CrossEncoder` 将请求分类为代码 (`logic_king`)、数学 (`deepthink`)、创意 (`vibes_master`) 等，选择最专精的模型。
*   **Prompt 注入**：针对数学推理任务自动注入 LaTeX 格式规范。
*   **OpenWebUI 优化**：智能识别后台任务（如标题生成），避免污染对话界面。

## 🛠️ 快速开始

1.  **安装依赖**：
    ```bash
    pip install -r requirements.txt
    ```
2.  **配置**：
    修改 `main.py` 中的 `Config` 类，填入你的聚合 API Key 和 Base URL。
3.  **启动**：
    ```bash
    python main.py
    ```
    服务将在 `http://0.0.0.0:2000` 启动。

## 🧠 路由逻辑

*   **逻辑/代码** -> `gemini-3-pro-preview`
*   **数学/推理** -> `gemini-3-pro-deepthink`
*   **创意写作** -> `MiniMax-M2`
*   **兜底/复杂视觉** -> `gpt-5.1`

## 感谢gemini!
